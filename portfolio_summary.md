# 1. 프로젝트 한 줄 소개
"AI가 예산과 선호도를 분석해 최적의 맞춤형 여행 코스를 1분 만에 설계해주는 지능형 여행 플래너 앱, Tripify"

---

# 2. 프로젝트 개요
- **프로젝트명**: Tripify
- **개발 기간**: (해당 기간 작성 필요, 예: 2024.11 ~ 2024.12)
- **팀 구성**: 2명 (기획/디자인 공동 진행)
  - **내 역할**: 백엔드 (Django/DRF) 서버 및 데이터베이스 구축, 인증 구현, 외부 데이터 연동(공공데이터, OAuth), AI (Claude) 기반 일정 자동 생성 로직 설계 및 구현
- **서비스 한 줄 설명**: 사용자의 예산, 여행 일정, 선호 스타일을 기반으로 실제 데이터(관광지, 축제, 호텔)를 활용해 AI가 자동으로 최적의 여행 일정을 생성하고 관리할 수 있도록 돕는 서비스입니다.

---

# 3. 사용 기술 스택
- **Back-end**: Python 3, Django 5.2.9, Django REST Framework, DRF Token, python-dotenv
- **Front-end**: Vue 3 (Vite), Pinia, Vue Router, JavaScript, TailwindCSS(또는 사용하신 CSS 프레임워크)
- **Database**: PostgreSQL (배포/운영), SQLite3 (개발)
- **AI & 3rd Party API**: SSAFY GMS API (Claude Haiku 4.5), Kakao/Google/Naver OAuth 2.0, Gmail SMTP, Kakao Maps API 

---

# 4. 핵심 기능
- **AI 맞춤형 여행 코스 자동 생성**: 지역, 예산, 일정, 여행 스타일 등을 입력하면 AI가 실제 관광/음식/숙박 데이터를 조합해 최적의 일별 방문 코스, 예상 비용, 식사 계획을 포함한 일정을 자동 생성합니다.
- **자연어 기반 AI 일정 유연한 수정**: 생성된 일정에 대해 "둘째 날 점심은 해산물로 바꿔줘" 같은 자연어 요구사항을 입력하면 예산을 재배분하여 일정을 즉시 수정해 줍니다.
- **다중 소셜 간편 로그인 및 자체 인증 시스템**: 구글, 카카오, 네이버 기반의 OAuth 소셜 로그인과, 이메일 인증 코드를 활용한 자체 회원가입/비밀번호 찾기 기능을 지원합니다.
- **여행지 및 축제 검색/북마크 기능**: 한국관광공사 데이터를 활용해 카테고리별 장소 필터링과 현재 열리는 축제를 탐색하고, 위시리스트와 북마크를 통해 개인화된 장소를 관리할 수 있습니다.

---

# 5. 내가 담당한 기능 및 구현 내용
**① AI 기반 여행 일정 자동 생성 엔진 로직 설계 및 구현**
- **문제**: 정형화된 관광지 정보 데이터베이스와 비정형 AI 생성 결과를 매끄럽게 결합하여 퀄리티 높은 여행 코스를 만들어야 했습니다.
- **해결**:
  - `GeminiService`(Claude Haiku 4.5 활용) 파이프라인을 구축하여 사용자 입력(예산, 인원, 여행 스타일 등)을 프롬프트로 변환했습니다.
  - 생성된 응답을 정형화된 JSON 형태로 강제하여 백엔드 DB 구조(`Itinerary`, `TravelPlan` 모델)에 바로 파싱 및 매핑할 수 있도록 설계했습니다.
  - JSONField를 적극 활용하여, 일별 세부 관광지 배열, 소요 비용 계산식, 교통 수단을 효율적으로 저장하도록 DB 스키마를 최적화했습니다.
- **결과**: 사용자가 조건을 입력하면 평균 10초 이내에 실제 데이터베이스와 연동된 일별 이동 경로 및 예산 검증이 끝난 완벽한 여행 일정을 응답받을 수 있도록 구현했습니다.

**② 자연어를 통한 AI 일정 동적 수정 기능 구현**
- **문제**: 이미 생성된 일정 전체를 재생성하면 API 비용 낭비가 심하고 사용자가 기존에 만족하던 일정부분까지 변경되는 문제가 있었습니다.
- **해결**:
  - 기존의 `TravelPlan` 엔티티와 일별 `Itinerary` 데이터를 컨텍스트로 함께 전달하고, 변경된 요구사항만 반영해 차이점(Patch)을 반환하는 프롬프트 엔지니어링을 적용했습니다.
  - 수정된 일정의 예산이 초기 설정된 총예산을 초과하지 않는지 서버 단에서 교차 검증하는 비즈니스 로직을 추가했습니다.
- **결과**: 불필요한 전체 일정 재생성을 막아 API 호출 비용 단가를 낮추면서, 원래 계획의 뼈대는 유지한 채 사용자가 원하는 일부 스케줄만 성공적으로 교체하도록 개선했습니다.

**③ 통합 인증 서버 구축 (OAuth 2.0 & SMTP)**
- **문제**: 사용자 접근성을 위해 소셜 로그인을 도입하면서 동시에 기존의 이메일 회원가입 사용자도 동일한 로직에서 관리해야 했습니다.
- **해결**: 
  - 커스텀 `User` 모델을 설계해 `login_type`(normal, kakao, google, naver)으로 인증 출처를 관리했습니다.
  - 자체 이메일 가입자는 Gmail SMTP를 통해 `EmailVerificationToken` 생성 및 메일 전송 로직을 비동기(또는 백그라운드)로 처리해 이메일 인증을 강제했습니다.
  - 소셜 로그인은 인가 코드(Code)를 통해 Access Token을 발급받은 뒤, 백엔드에서 자체 DRF Session Token을 랩핑해 클라이언트로 반환하는 방향으로 통합했습니다.
- **결과**: 보안성을 높이고 서로 다른 플랫폼의 사용자 인증 정보를 하나의 일관된 DRF Token 체계로 유연하게 관리할 수 있었습니다.

---

# 6. 기술적 도전 및 트러블슈팅
**[도전] AI 프롬프트 생성 결과물이 일관되지 않아 JSON 파싱 오류(KeyError 등)가 잦은 문제**
- **원인/어려움**: LLM 특성상 결과 포맷이 일관되지 않거나 마크다운 형식을 섞어 응답하는 경우가 발생해 백엔드 API 서버가 역직렬화(Deserialization)에 실패하는 빈도가 높았습니다.
- **해결 방법**: 
  - 시스템 프롬프트(System Prompt)에 명시적으로 `JSON-only` 반환 제약 조건을 강력하게 주입하고 템플릿 예시(Few-shot)를 제공했습니다.
  - 응답에 Markdown 코드 블록이 포함되어 올 경우, 정규식을 활용해 순수 JSON 텍스트만 추출하도록 전처리(Pre-processing) 로직을 추가했습니다.
  - 파싱 실패 시, 기본 안전 구조(Fallback Dummy Data)를 반환하도록 예외 처리를 둬서 서버 크래시를 방지했습니다.

**[도전] DRF Token의 보안성 이슈와 프론트엔드 상태 동기화 문제**
- **원인/어려움**: Access Token의 만료 기간 통제가 어려웠고, 장시간 로그인 시 보안 취약점이 우려되었습니다.
- **해결 방법**: 토큰 기반 통신에 타임아웃 룰을 부여하고, 백엔드에서 인증 확인 시 `last_login` 혹은 인터셉터 로직을 활용해 **6시간 지속 후 자동 로그아웃** 되는 보안 정책을 수립했습니다. 클라이언트 측(Axios 401 Interceptor)에서도 만료 시 localStorage를 비우고 로그인 뷰로 튕겨내도록 프론트엔드 담당자와 협의해 데이터 정합성을 맞췄습니다.

---

# 7. 성과 및 배운 점
- **성과**: 
  - API 설계부터 DB 모델링, AI 로직 결합까지 전체 백엔드 시스템을 1인 체제로 기한 내 성공적으로 구축했습니다.
  - 단순 CRUD API를 넘어 LLM(Claude)과 Relational Database를 연결하는 하이브리드 비즈니스 로직을 구축한 경험을 정립했습니다.
- **배운 점**:
  - 인증(Authentication)과 인가(Authorization)의 명확한 차이와 OAuth2 작동 원리를 깊게 이해했습니다.
  - 프론트엔드 개발자와 협업하며 "클라이언트 입장에서 사용하기 편한 직관적인 JSON 응답 구조"를 설계하는 RESTful API 디자인의 중요성을 체감했습니다.
  - 추후 분산 처리 시스템(Celery/Redis 등)을 통해 무거운 AI 호출 함수나 이메일 전송을 비동기 메시지 큐로 뺌으로써 응답 속도를 향상시킬 수 있는 아키텍처적 개선안도 도출하게 되었습니다.
